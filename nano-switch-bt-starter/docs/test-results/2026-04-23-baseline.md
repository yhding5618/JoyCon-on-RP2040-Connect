# Switch Pairing Baseline - 2026-04-23

## Test

- Test ID: T00
- Purpose: Baseline build and runtime log capture before isolated changes.

## Repo State

- Git commit: `29785911eea60f93178a1bb01019b5a430ed45e4`
- Working tree notes: `TEST_PLAN_SWITCH_PAIRING.md`, `_refs/`, and `.vscode/` are untracked in this checkout.

## Build Environment

- ESP-IDF path: `C:\Espressif\frameworks\esp-idf-v4.4.8`
- ESP-IDF version reported by export: `v4.4.8-dirty`
- IDF tools path needed on this machine: `C:\Espressif`
- Build result: pass, user reported build completed with no problem
- Compiler warnings: none reported by user

## Firmware / Sketch

- NINA firmware: `nina_firmware/`, flashed successfully by user
- RP2040 sketch: pending confirmation, expected `rp2040_bridge/rp2040_bridge.ino`

## Manual Test Results

- PC discoverable: not tested in this baseline pass
- Switch discoverable: yes, inferred from `ACL_CONN_CMPL` and `AUTH_CMPL`
- Pass level reached: Pass Level 1, with GAP authentication complete but HID open failing before Pass Level 2

## Required Baseline Evidence

- `REGISTER_APP status`: `0x00`
- `REGISTER_APP in_use`: not decoded in this firmware/log output
- `OPEN status`: `0x01`
- `OPEN conn_status`: `0x02`
- `GAP disconnect reason`: `0x13`
- `SET_REPORT / GET_REPORT / INTR_DATA observed`: no

## Commands / Logs

```text
clear-bonds:
STATUS flags=0x07 (BRIDGE_READY|BT_READY|HID_READY) ... last_hid_event=REGISTER_APP last_hid_status=0x00 ... last_gap_event=CONFIG_EIR_DATA ... bonds=0

pre-switch status:
STATUS flags=0x07 (BRIDGE_READY|BT_READY|HID_READY) ... last_hid_event=REGISTER_APP last_hid_status=0x00 ... bonds=0

post-switch status:
STATUS flags=0x07 (BRIDGE_READY|BT_READY|HID_READY) ... last_error=0x01 ... last_hid_event=OPEN last_hid_status=0x01 last_hid_conn_status=0x02 ... last_gap_event=ACL_DISCONN_CMPL last_gap_reason=0x13 bonds=1

event summary:
EVENT seq=8  source=GAP_CALLBACK event=ACL_CONN_CMPL status=0x00
EVENT seq=9  source=GAP_CALLBACK event=AUTH_CMPL status=0x00
EVENT seq=10 source=GAP_CALLBACK event=ACL_DISCONN_CMPL reason=0x13
EVENT seq=11 source=HID_CALLBACK event=OPEN status=0x01 conn=0x02
EVENT seq=12 source=GAP_CALLBACK event=ACL_CONN_CMPL status=0x00
EVENT seq=13 source=GAP_CALLBACK event=AUTH_CMPL status=0x00
```

## Notes

- Baseline build should be run by the user per test-session agreement.
- If using PowerShell directly, set `IDF_TOOLS_PATH=C:\Espressif` before exporting ESP-IDF so the installed toolchains are found.
- Baseline failure signature is below app-layer Joy-Con subcommands because no `SET_REPORT`, `GET_REPORT`, or `INTR_DATA` callback occurred.

## T01 Cleanup Probe

Observed cleanup events:

```text
VC_UNPLUG status=0x00 conn=0x02
CLEAR_BONDS_BEGIN bonds_before=1
REMOVE_BOND status=0x00 index=0
REMOVE_BOND_DEV_COMPLETE arg0=0x00 arg1=0x00
```

The probe was not a clean "before Switch test" sample because the Switch kept reconnecting during/after cleanup:

```text
ACL_CONN_CMPL status=0x00
AUTH_CMPL status=0x00
final status bonds=1
```

Repeat T01 with the Switch out of `Change Grip/Order` before cleanup, then verify the device remains at `bonds=0` before opening the Switch pairing screen.

Clean T01 repeat result:

```text
VC_UNPLUG status=0x00 conn=0x02
CLEAR_BONDS_BEGIN bonds_before=1
REMOVE_BOND status=0x00 index=0
REMOVE_BOND_DEV_COMPLETE arg0=0x00 arg1=0x00
final status bonds=0
no AUTH_CMPL after cleanup
```

T01 pass conclusion:

- virtual-cable unplug completes
- bond cleanup completes
- pre-Switch-test state can be made clean with `bonds=0`
- no stale host reconnect occurred in the clean sample

## T02 Identity Logging Patch

Instrumentation added for the next build:

- new `BT_IDENTITY` event source in the UART event ring
- identity snapshots after base-MAC spoofing, controller init, Bluedroid enable, HID register callback, and GAP identity configuration
- decoded fields in `host_tools/send_state.py dump-events`: BT address byte pairs, intended base MAC byte pairs, CoD status/major/minor/service, HID subclass, descriptor length, and string lengths
- event ring expanded from 20 to 128 entries so the identity snapshots and pairing failure can coexist in one dump

Initial T02 runtime result:

```text
actual BT address after Bluedroid enable: D4:F0:57:4D:4E:32
intended base MAC: D4:F0:57:4D:4E:30
MAC prefix: D4:F0:57 confirmed
GAP name: Joy-Con (L)
HID service: Wireless Gamepad
HID description: Gamepad
HID provider: Nintendo
HID subclass: 0x08
current descriptor length: 0x00AA / 170 bytes
final cleanup status: bonds=0
```

The suspicious field is CoD. Immediate snapshots after `configure_gap_identity()` still showed:

```text
cod_err=0x00
major=0x1f
minor=0x00
service=0x000
```

This is `Uncategorized`, not Peripheral/Gamepad-like. Because ESP-IDF queues `esp_bt_gap_set_cod()` through the BT task, the next T02 patch adds GAP identity API return-code events and a 500 ms delayed `after_gap_identity_settled` snapshot before classifying this as a true CoD failure.

Settled T02 runtime result:

```text
GAP identity API return codes: set_name_err=0x00 set_cod_err=0x00 set_scan_mode_err=0x00
after_gap_identity_settled:
  BT address: D4:F0:57:4D:4E:32
  intended base MAC: D4:F0:57:4D:4E:30
  CoD: major=0x05 minor=0x02 service=0x000
  HID subclass: 0x08
  descriptor length: 0x00AA / 170 bytes
  strings: Joy-Con (L), Wireless Gamepad, Gamepad, Nintendo
```

T02 pass conclusion:

- actual Bluetooth address uses the Nintendo-like `D4:F0:57` prefix
- GAP/HID strings match the planned Switch-facing identity
- HID subclass is `0x08`
- CoD settles to Peripheral/Gamepad-like after the asynchronous GAP update completes
- current descriptor length is documented as 170 bytes, which differs from the 203-byte `joycontrol` descriptor planned for T04

## T03 SDP Dump Setup

Added:

- `tools/dump_sdp.sh`
- `docs/sdp-checklist.md`

T03 runtime result: pending Linux SDP dump.

Initial T03 runtime result:

```text
Bluetooth address: D4:F0:57:4D:4E:32
Service Name: Wireless Gamepad
Service Description: Gamepad
Service Provider: Nintendo
Service Class ID: Human Interface Device (0x1124)
Protocol Descriptor List: L2CAP PSM 17 / 0x0011, HIDP
Profile Descriptor: Human Interface Device (0x1124), Version 0x0100
```

The first `sdptool browse` output did not show:

```text
Additional Protocol Descriptor List / interrupt PSM 0x0013
HIDDeviceSubclass
HIDVirtualCable
HIDReconnectInitiate
HIDDescriptorList
```

T03 status: incomplete and suspicious. Updated `tools/dump_sdp.sh` to capture more `sdptool` formats before deciding whether the record itself is missing the interrupt/descriptor fields or the default BlueZ browse view is hiding them.

Fuller T03 runtime result:

```text
AdditionalProtocolDescriptorLists: L2CAP PSM 0x0013, HIDP
HIDDeviceReleaseNumber: 0x0100
HIDParserVersion: 0x0111
HIDDeviceSubclass: 0x08
HIDCountryCode: 0x21
HIDVirtualCable: true
HIDReconnectInitiate: true
HIDDescriptorList starts: 05 01 09 05 A1 01 06 01 FF 85 21 09 21 75 08 95
HIDDescriptorList length: 170 bytes
```

T03 conclusion:

- stock `esp_hidd` does expose both HID control and interrupt PSMs
- core strings, subclass, virtual cable, reconnect initiate, and descriptor attribute are present
- current descriptor does not match the 203-byte `joycontrol` descriptor
- HID country code is `0x21`, not the checklist value `0x00`

## T04 Joycontrol Descriptor Patch

Patch added:

- `SWITCH_HID_USE_JOYCONTROL_DESCRIPTOR` feature flag set to `1`
- existing 170-byte descriptor preserved as `kCurrentDescriptor`
- 203-byte `joycontrol` descriptor added as `kJoycontrolDescriptor`
- HID app registration now uses `SWITCH_HID_DESCRIPTOR`
- `_Static_assert(sizeof(kJoycontrolDescriptor) == 203u, ...)`

T04 runtime result: pending rebuild/flash and Switch pairing test.

T04 runtime result:

```text
descriptor length: 0x00CB / 203 bytes
settled CoD: major=0x05 minor=0x02
pre-Switch cleanup: bonds=0
Switch attempt:
  ACL_CONN_CMPL status=0x00
  AUTH_CMPL status=0x00
  ACL_DISCONN_CMPL reason=0x13
  HID OPEN status=0x01 conn=0x02
  SET_REPORT / GET_REPORT / INTR_DATA observed: no
```

T04 conclusion:

- the `joycontrol` descriptor is active
- descriptor-only did not improve the failure level
- failure signature remains below app-layer Joy-Con subcommands
- T05 is not meaningful yet because the Switch still never reaches HID report traffic
- continue to T07 full SDP difference test

## T07 SDP Difference Setup

Added:

- `docs/reference/joycontrol-sdp-record-hid.xml`
- `tools/compare_sdp.py`

T07 runtime result: pending fresh SDP dump from the T04 firmware.

Initial T07 compare output:

```text
Summary: 5 difference(s)
BrowseGroupList: empty vs 0x1002
HIDCountryCode: 0x21 vs 0x00
HIDDescriptorList length: 0x00 vs 0xCB
HIDDescriptorList first 16 bytes: empty vs joycontrol prefix
HIDDescriptorList SHA256: missing vs joycontrol hash
```

The `HIDDescriptorList length: 0x00` result conflicts with T04 firmware telemetry, which reported descriptor length `0xCB / 203`. Treat this first T07 compare as parser/sample-suspect, not a firmware conclusion. Updated `tools/compare_sdp.py` to:

- accept descriptor hex with or without `encoding="hex"`
- fall back to raw `sdptool` `Data ...` descriptor output
- report `BrowseGroupList` without counting it as a decision-rule mismatch

T07 runtime result: pending rerun with updated comparison script.

Second T07 compare output still reported:

```text
HIDDescriptorList length: 0x00 vs 0xCB
HIDDescriptorList first 16 bytes: empty vs joycontrol prefix
HIDDescriptorList SHA256: missing vs joycontrol hash
```

This means the comparison input file still did not expose descriptor bytes to the parser. Updated `tools/compare_sdp.py` again with a global descriptor-like hex scan and a report note when no `0x0206` descriptor bytes are found.

T07 runtime result: pending verification that the Linux SDP dump file contains attribute `0x0206` from the T04 firmware.

Third T07 compare output again reported missing NINA descriptor bytes, and the script emitted:

```text
Note: no HID descriptor bytes were found in the NINA input file.
```

This means T07 is blocked on obtaining a fresh/full `sdptool` dump that contains attribute `0x0206`. Updated `tools/dump_sdp.sh` to warn when the saved dump lacks `DescriptorList` / `0x0206`.

Follow-up grep check:

```text
grep for 0x0206 / DescriptorList / joycontrol descriptor prefix returned no matches
```

This confirms the current Linux-side SDP input file is incomplete or stale. T07 remains blocked until `tools/dump_sdp.sh` produces a dump containing the HID descriptor section.

Fresh full T07 dump from Linux still omitted `0x0206`:

```text
records --xml includes attributes 0x0200, 0x0201, 0x0202, 0x0203, 0x0204, 0x0205,
then skips 0x0206 and resumes at 0x0207.
HIDCountryCode remains 0x21.
```

Local ESP-IDF inspection found the likely cause:

```text
components/bt/host/bluedroid/common/include/common/bt_target.h:
  SDP_MAX_PAD_LEN = 300

components/bt/host/bluedroid/stack/hid/hidd_api.c:
  country_code = 0x21
```

The 203-byte Joy-Con descriptor becomes an SDP attribute of about 211 bytes. With earlier HID attributes already stored in the same record pad, stock `SDP_MAX_PAD_LEN = 300` is too small, so `SDP_AddAttribute(... ATTR_ID_HID_DESCRIPTOR_LIST ...)` fails and the descriptor is absent from the published SDP record.

Added `host_tools/patch_idf_hid_switch_compat.ps1` to patch local ESP-IDF:

```text
SDP_MAX_PAD_LEN: 300 -> 512
HIDCountryCode: 0x21 -> 0x00
```

T07 runtime result: pending patched-IDF rebuild and fresh SDP comparison.

Patched-IDF SDP dump result:

```text
BrowseGroupList: present, 0x1002
Control PSM: 0x0011
Interrupt PSM: 0x0013
HIDDeviceSubclass: 0x08
HIDCountryCode: 0x00
HIDVirtualCable: true
HIDReconnectInitiate: true
HIDDescriptorList: present
HIDDescriptorList prefix: 05 01 15 00 09 04 A1 01 85 30 05 01 05 09 19 01
HIDDescriptorList length: 0xCB / 203 bytes
```

T07 status: patched SDP record now matches the expected high-risk fields. Formal `tools/compare_sdp.py` output is pending.

Formal T07 compare result:

```text
Summary: match
Control PSM: match
Interrupt PSM: match
HIDDeviceSubclass: match
HIDCountryCode: match
HIDVirtualCable: match
HIDReconnectInitiate: match
HIDDescriptorList length: 0xCB match
HIDDescriptorList SHA256: 25f0b3b7e59bdfec05e8cced16e43a8878509865a0cb223f05025c556f3bedba match
```

T07 conclusion:

- the patched ESP-IDF build now publishes a Joy-Con-compatible HID SDP record for all fields checked by `tools/compare_sdp.py`
- proceed to a fresh Switch pairing attempt before debugging app-layer subcommands further

Post-T07 Switch pairing retest:

```text
pre-Switch cleanup: bonds=0
ACL_CONN_CMPL status=0x00
AUTH_CMPL status=0x00
ACL_DISCONN_CMPL reason=0x13
HID OPEN status=0x01 conn=0x02
SET_REPORT / GET_REPORT / INTR_DATA observed: no
```

The failure signature is unchanged even after SDP matches the `joycontrol` reference. This means T05/T08 are still blocked; the failure is below app-layer Joy-Con subcommands.

Next diagnostic:

- patch ESP-IDF `bta_hd_open_failure()` so the raw HID/L2CAP close reason is preserved in the existing `ESP_HIDD_OPEN_EVT` fields
- rerun the same Switch pairing test and decode the new `OPEN status=... conn=...` pair as the raw lower/upper bytes of the HID reason

First raw-reason diagnostic result:

```text
pre-Switch cleanup: bonds=0
ACL_CONN_CMPL status=0x00
AUTH_CMPL status=0x00
ACL_DISCONN_CMPL reason=0x13
HID OPEN status=0x01 conn=0x00
SET_REPORT / GET_REPORT / INTR_DATA observed: no
```

The old generic HID connection state `conn=0x02` is gone, so the ESP-IDF patch is active. However, `status=0x01 conn=0x00` still collapses either raw HID reason `0x0000` or `0x0001`, so one more diagnostic is needed. Update the ESP-IDF patch to encode clean peer L2CAP disconnects as:

```text
OPEN status = 0x11 for HID control PSM, or 0x13 for HID interrupt PSM
OPEN conn   = HID connection state at disconnect
```

Second raw-reason diagnostic result:

```text
pre-Switch cleanup: bonds=0
ACL_CONN_CMPL status=0x00
AUTH_CMPL status=0x00
ACL_DISCONN_CMPL reason=0x13
HID OPEN status=0x11 conn=0x02 psm=CTRL close_state=CONNECTING_INTR
SET_REPORT / GET_REPORT / INTR_DATA observed: no
```

Interpretation:

- the HID control PSM reached the point where the stack was waiting for the interrupt PSM
- the Switch closed the control PSM before opening interrupt
- SDP, HID descriptor identity, and app-layer Joy-Con subcommands are no longer the active suspect

Next experiment:

- patch ESP-IDF `hidd_conn.c` so the HID device proactively initiates interrupt PSM after inbound control configuration completes
- allow the resulting inbound-control/outbound-interrupt hybrid L2CAP confirmation path

Inbound-control/outbound-interrupt patch result:

```text
pre-Switch cleanup: bonds=0
status flags: BRIDGE_READY|BT_READY|HID_READY|CONNECTED|VIRTUAL_CABLE
last_error: 0x00
last_hid_event: SEND_REPORT
last_hid_status: 0x00
last_hid_report_type: 0x04
last_hid_report_id: 0x30
last_report_id: 0x10
last_subcommand: 0x30
SET_REPORT / GET_REPORT / INTR_DATA observed: yes
INTR_DATA report_id=0x10 len=0x09 observed repeatedly
SEND_REPORT report_id=0x30 observed repeatedly
```

Conclusion:

- the Switch reaches Pass Level 3 and at least part of Pass Level 4
- the stock ESP-IDF HID device path needed a Switch-specific L2CAP behavior patch: initiate interrupt PSM after inbound control config
- T05 is now meaningful because app-layer subcommand exchange is active

T05 change:

- update `GET_DEVICE_INFO` reply length from 11 bytes to 12 bytes
- append the second trailing `0x01` byte expected by the Joy-Con-style device-info reply

T05/T08 runtime result:

```text
status flags: BRIDGE_READY|BT_READY|HID_READY|CONNECTED|VIRTUAL_CABLE
last_error: 0x00
last_subcommand: 0x30
last_hid_event: SEND_REPORT
last_hid_status: 0x00
INTR_DATA report_id=0x10 len=0x09 observed repeatedly
SEND_REPORT report_id=0x30 observed repeatedly
```

Functional input result:

```text
python host_tools\send_state.py state COM3 --buttons 0x30
python host_tools\send_state.py state COM3 --buttons 0x0
```

`0x30` is `SB_BTN_LJC_SL | SB_BTN_LJC_SR`, the left Joy-Con Change Grip/Order pairing combo. The Switch accepted the controller after this input sequence.

T08 status: pass, Pass Level 5 reached.
